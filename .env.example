# =============================================================================
# MCP AI COMMIT - ENVIRONMENT CONFIGURATION EXAMPLE
# =============================================================================
# Copy this file to .env and customize for your setup

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection settings
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ai_commit
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here

# Schema for AI commit tables (will be created if it doesn't exist)
AI_COMMIT_SCHEMA=ai_commit

# =============================================================================
# AI MODEL CONFIGURATION
# =============================================================================
# OpenAI API configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API configuration (optional)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Default AI settings
AI_COMMIT_DEFAULT_PROVIDER=openai
AI_COMMIT_DEFAULT_MODEL=gpt-4
AI_COMMIT_TEMPERATURE=0.3
AI_COMMIT_MAX_TOKENS=200

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# Validation level: strict, warn, permissive
AI_COMMIT_VALIDATION_LEVEL=strict

# Execution limits
AI_COMMIT_MAX_FILES=50
AI_COMMIT_MAX_CHANGES=1000

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Logging
AI_COMMIT_LOG_LEVEL=INFO

# Performance
AI_COMMIT_MAX_CONCURRENT=10

# Data directory for local storage
AI_COMMIT_DATA_DIR=~/.mcp-ai-commit

# =============================================================================
# OPTIONAL: ADVANCED CONFIGURATION
# =============================================================================
# Custom OpenAI endpoint (for self-hosted models)
# OPENAI_BASE_URL=http://localhost:8000/v1

# =============================================================================
# USAGE INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the values above with your actual configuration
# 3. Ensure your PostgreSQL database is running and accessible
# 4. The ai_commit schema will be created automatically on first run
# 5. Keep your .env file secure and never commit it to version control

# =============================================================================
# DATABASE SETUP COMMANDS
# =============================================================================
# To create a dedicated database for AI commit:
# 
# psql -U postgres -c "CREATE DATABASE ai_commit;"
# psql -U postgres -c "CREATE SCHEMA ai_commit;" ai_commit
# 
# Or use your existing database with a custom schema name